# Migration `20201117041906-create-review`

This migration has been generated by Juliana Amoasei at 11/17/2020, 1:19:06 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Review` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `postId` INT NOT NULL,
    `reviewerId` INT NOT NULL,
    `aprovado` BOOLEAN NOT NULL DEFAULT false,
    `nota` INT NOT NULL,

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Review` ADD FOREIGN KEY (`postId`) REFERENCES `Post`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `Review` ADD FOREIGN KEY (`reviewerId`) REFERENCES `User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201117041906-create-review
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,43 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model posts {
+  id         Int       @id @default(autoincrement())
+  titulo     String
+  conteudo   String
+  autorId    Int
+  publicado  Boolean?
+  created_at DateTime? @default(now())
+  autor      users      @relation(fields: [autorId], references: [id])
+
+  @@index([autorId], name: "autorId")
+  @@map(name: "Post")
+}
+
+model users {
+  id         Int       @id @default(autoincrement())
+  nome       String
+  email      String
+  created_at DateTime? @default(now())
+  posts      posts[]
+
+  @@map(name: "User")
+}
+
+model reviews {
+  id            Int      @default(autoincrement()) @id
+  postId        Int
+  post          posts     @relation(fields: [postId], references: [id])
+  reviewerId    Int
+  reviewer      users     @relation(fields: [reviewerId], references: [id])
+  aprovado      Boolean  @default(false)
+  nota          Int
+
+    @@map(name: "Review")
+}
```


